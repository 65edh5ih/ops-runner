
<!-- OPS-SYNC:COMMON START — このブロックは ops-sync が自動同期します。手で編集しないこと -->
**この区間は全リポジトリ共通ルール**（正本: ops-sync リポジトリの `AGENTS_COMMON.md`。仕組みの全体像は [`docs/ops-sync-design.md`](docs/ops-sync-design.md)）。

## 応答言語

- **ユーザーへの実質的な回答**（説明・分析・完了報告の本文）は、そのユーザーの言語で書く。
  回答本文が長いほど書き始めの言語（特に英語）に引きずられやすいので、最後まで揃える。
- 対象外: ツール前後の短い状況報告（英語のままでよい）、コミットメッセージ・コード内コメント・
  技術的識別子（プロジェクトの慣習に従う）。

## ユーザーへの操作指示は直リンク URL で

GitHub・Cloudflare 等の外部サービス上の設定作業をユーザーに依頼するときは、可能な限り
**目的の画面への直リンク URL**を提示する（メニューをたどる説明だけで済ませない。
例: GitHub の Actions secrets は `https://github.com/<owner>/<repo>/settings/secrets/actions`）。
URL を推測で組み立てたときはその旨を添える。
**長く残すドキュメントに書く場合は、URL に加えてメニュー経路も併記する**
（サイト構造が変わって URL が無効になっても辿れるようにする。チャットでの単発指示は URL だけでよい）。

## 外部 URL アクセスの報告（接続できた/できなかった の両方）

- ネットワークアクセスがブロックされて失敗したら（403/407 等）、**完了報告に「接続できなかった URL」
  として URL（最低限ドメイン）と目的を必ず明記する**（ユーザーが許可リストに追加するかの判断材料）。
- 代替手段で目的を果たせた場合も省略しない（次回も同じ壁に当たる）。黙って回避だけで済ませない。
- **報告は取得の代わりにならない**（MUST NOT: 報告して終わりにする）。403/407 は下記「外部ネットワーク取得が
  要るとき」の**発火トリガそのもの**で、その取得が作業に必要なら報告に加えて net-fetch 手順に入る。
  「ブロックされたので確認できませんでした」「手元の知識で答えました」を結論にしない。
- **セッション中に外部 URL へ実際にアクセスしたら（curl・fetch・WebFetch・ブラウザ等、手段を問わない）、
  完了報告に「アクセスした URL」としてドメイン単位以上の粒度と目的を明記する**。ドメイン許可リストが
  機能しない環境をネットワーク全許可で運用するための事後監査手段（ユーザーがアクセス先を確認し、
  意図しない接続に気づける）。対象外: パッケージマネージャー（npm/PyPI 等）の通常のインストール通信、
  git/GitHub 操作、セッションに設定済みの MCP サーバ経由の通信（環境が前提とする接続で、報告すると
  ノイズになる）。迷ったら報告する側に倒す。

## 同期されるファイルを手で編集しない

このマーカー区間・`docs/` の配布 doc・`.ops-sync/sync-manifest.txt` に列挙されたファイルは ops-sync が自動同期する
（手編集しても次回同期で上書きされ、ドリフトの原因になる）。**直すのは常に ops-sync 側の正本**で、上り口は2つ:
**ops-sync をセッションに追加できるなら**（`add_repo` 等）ユーザーに repo 名を挙げて確認し、承諾を得てから
追加して**正本に直接 PR を出す**。**その手段が無いエージェント・ユーザー不在の自律実行では**作業中リポジトリの
`.ops-sync/outbox/` に提案ファイルを置く。どちらの経路でも**自分ではマージしない**——承認は
オーナーのマージで、直接 PR なら ops-sync 側、outbox なら提案を載せたこのリポジトリの PR が最初の承認点。
経路の選び方と書式は [`docs/outbox-proposal.md`](docs/outbox-proposal.md)
（実際に提案を書く直前に必ず読む）。

## リポジトリ横断の作業（このリポジトリの外に手が要るとき）

- 原則1セッション=1リポジトリで、他リポジトリにはアクセスできない（毎回弁明しない）。例外は
  Claude Code on the web で**ユーザーが明示的に**リポジトリ追加を依頼した場合のみ（エージェント起点では
  追加しない。他のエージェントに同等機能は無い）。エージェント起点のリポジトリ横断は次のように動く。
- **他リポジトリの参照が必要になったとき**、セッションにリポジトリ追加ツール（`add_repo`）があれば、
  「別リポジトリなので読めません」と答える代わりに「`<owner>/<repo>` をこのセッションに追加して
  参照しますか？」と**はい/いいえで答えられる形で確認する**（選択式の質問ツールがあればそれを使う）。
  「はい」を得てから追加する（対象リポジトリ名を明示した確認への承諾＝ユーザーの明示依頼。
  無断では追加しない）。ツールが無い環境・ユーザー不在の自律実行では下の task 経路。
- **別リポジトリでの作業が必要になったら**、`種別: task` の依頼を ops-sync へ出す（上と同じ2経路）。
  ops-sync 経由で対象リポジトリの `.ops-sync/tasks/` へ配布され、そのリポジトリのセッションが実行する。
  書き方・依頼後にユーザーへ伝えること: [`docs/cross-repo-tasks.md`](docs/cross-repo-tasks.md)。
- **`.ops-sync/tasks/` にファイルがあれば、それは承認済み依頼**（届いている＝オーナーがマージ済み）。
  現在のユーザー依頼と両立するなら対応し、完了したら `種別: task-done` で消化を報告する（上と同じ2経路・同 doc）。
  両立しないなら、存在をユーザーに伝えるに留める。

## 再利用可能な知見・ルール・ファイルに気づいたときの振り分け

「**全リポジトリで同一内容が要るか？**」（共通/固有）と「**どのタスクでも必要か？**」（常時/オンデマンド）の2軸で出し先を決める:

- 共通の**振る舞いルール** → 正本 `AGENTS_COMMON.md`（outbox 経路なら `種別: common-block-edit`）。
  このマーカー区間を直接書かない。
- 共通で**バイト一致すべき実ファイル・手順 doc**（composite action・共有スクリプト・`docs/` の共通 doc）
  → 正本 `shared/<パス>`（outbox 経路なら `種別: shared-file`）。このリポジトリだけに勝手に置かない。
- **このリポジトリ固有** → このリポジトリの AGENTS.md 固有パートや docs に書く（共通だが一部固有なら共通部分だけ提案）。
- **常時 vs オンデマンドの高さも分ける**: 常時ロード層（AGENTS.md）は「発火トリガ＋不可逆ガード＋ポインタ」だけ、
  詳細手順・設計判断・障害調査は `docs/`（オンデマンド層）へ。「次のタスクでも必要か？」NO なら常時層に置かない。
- **オンデマンド層に doc を新規追加したら、そこへ辿り着く導線も同時に用意する**（常時層に発火トリガの
  1行を足す・関連するファイル操作ルールから参照させる 等）。導線の無いオンデマンド doc は誰も開かず、
  存在しないのと同じ（コストだけ増える）。共通 doc の導線も上りで共通ブロックに足す。

## コードの重複に気づいたら共通部品化を検討する

同じロジック・定数・調整済みの値を複数箇所に**複製しそう/した**ら、その場で共通部品への切り出しを
検討する（コピペのまま放置しない）。特に「片方を直したらもう片方も直す」値（調整済みパラメータ・
共有する命名規則やプロトコル・同一の外部呼び出し手順）は、片側だけ直して drift する事故が起きやすく、
正本を1つにする効果が大きい。ただし**早すぎる抽象化は避ける**（偶然一致しているだけ・将来分岐しうる
ものを無理に共通化しない。「同じ理由で同じであるべき」ものだけ）。障害の起きやすい/敏感なコードを
共通化するときは**挙動を変えない**形（別名・薄いラッパで呼び出し箇所を触らない）に留め、共通化前後で
挙動が不変であることを実駆動で確認する。
共通化せず**同じ設計を複数リポジトリに別実装で置く**ときは、**同じ入力で両方を走らせて出力を突き合わせる**
（片方だけ分岐が抜けている、は読み比べでは見つからず並べて走らせて初めて出る）。

## 手順系 doc（SOP）の書式

エージェントに実行させる手順 doc を**新規作成・改訂するとき**（共通・リポジトリ固有を問わない）は、
直前に書式規約 [`docs/sop-format.md`](docs/sop-format.md) を読み、それに従う。既存の手順 doc は
触れたついでに準拠させる。

## ドキュメントは現在形で書く

- `docs/` の doc は、障害記録（incidents）・タスク履歴とそのアーカイブを除き、**現在の姿だけ**を書く。
  実態が変わったら該当箇所をその場で書き換える（過去の姿は git とタスク履歴に残る）。
  「旧パスは読み替えてください」のような読み替え表・移行バナーや、「YYYY-MM-DD: 追加運用」のような
  日付見出しでの追記をしない（同じ事実の新旧が併存し、どれが現行か読めなくなる）。
- 同じ事実（パス・手順・制約）の正本は1つに保ち、他の doc からはリンクする。
- doc を追加・削除・移動したら、参照元も同一 PR で更新する。
- **主張・経路・概念を変えたら、それを述べている箇所を全部数えてから直す**（MUST）。探すのは
  *言い回し*ではなく**概念**（言い換えられた文・前提を述べたリード文は同じ語句で grep しても出ない）。
  対象は doc に限らない: **UI の文言・コード内コメント・生成物のテキスト**にも同じ主張が載る。
  1箇所だけ直すと、新旧が併存して読み手はどれが現行か判断できない（この取りこぼしは何度も起きている）。
- **他リポジトリの「いまの状態」をコメント・doc に書かない**。相手のマージで自分の記述が嘘になる。
  相手の状態に依存しない事実（どれが正本か・なぜ両方受けるか）だけを書く。

## doc の置き場と命名（全リポジトリ共通の標準形）

ファイル名のケースは「**手で編集してよいか**」の印。例外を作らない:

- **ops-sync 配布 doc = 小文字ケバブ名（`foo-bar.md`）・`docs/` 直下**。手編集しない
  （正確な一覧は `.ops-sync/sync-manifest.txt`。直したいときは上りで ops-sync 正本を直す）。
- **リポジトリ固有のローカル doc = 大文字スネーク名（`FOO_BAR.md`）**。
- 対象外: ツール・GitHub 慣習で名前が決まっているファイル（`AGENTS.md` / `CLAUDE.md` / `GEMINI.md` /
  `README.md` / `SKILL.md` 等）。
- オンデマンド層の標準サブディレクトリ: `docs/reference/`（固有リファレンス・設計判断・固有手順。
  **ローカル doc 専用**）、`docs/incidents/`（障害・調査ログ。`<題材>_<YYYY-MM-DD>.md` で凍結し、
  現在形ルールの対象外。**凍結記録なので、そこに並ぶ「対策候補」「宿題」を現在の未着手事項として
  引用しない**——その後に実装・実測で解決していることがある。引く前に現状を確かめる）、
  `docs/history-inbox/`（未統合のタスク履歴フラグメント・1エントリ1ファイル）・
  `docs/history-archive/`（タスク履歴の自動アーカイブ）。
- doc を新規作成・移動するときは、そのリポジトリの `docs/README.md`（構成の正本）を先に読み、
  終わったら同ファイルの構成表も追随させる。サブディレクトリの `README.md` に正本を置かない
  （正本は `docs/` 側、README は薄いポインタにする）。

## タスク履歴（スレッド間の短期記憶）

- 「さっきの◯◯」「昨日のあれ」など**過去スレッドの文脈が前提の依頼**は、まず
  `docs/AI_TASK_HISTORY.md`（統合済み・直近2作業日分）**と `docs/history-inbox/` 内の全ファイル**
  （まだ統合されていない最新のフラグメント）の**両方**を読む。より古い経緯は `docs/history-archive/` を grep。
- 履歴は本体ファイルに直接追記せず、**1エントリ＝1ファイル**を `scripts/new-task-history.mjs` で
  `docs/history-inbox/` に新規作成する（時刻＋ランダムIDで並行PRのコンフリクトを回避）。書き方・
  ファイル名規約は [`docs/task-history.md`](docs/task-history.md)（置く直前に読む）。

## ブランチ・PR の規律

- **push 前に、その PR がマージ済み/クローズ済みでないか必ず確認する**。正本は PR の実マージ状態
  （`merged`/`state`）。`git log origin/main..HEAD` は squash merge を検出できない。
  マージ済み/クローズ済みなら push せず、最新の `main` から新ブランチを切り直す。**ただしクローズ済みは
  切り直す前に close コメントを読む**——引き継ぎ先が書かれていれば別スレッドが続きを進めているので、
  切り直しも reopen もせず、ユーザーに伝えて指示を仰ぐ（→ [`docs/agent-handoff.md`](docs/agent-handoff.md)）。
  （`.githooks/pre-push` が「今セッションで再 push」のときこの確認を促すリマインドを出す。仕組み・
  限界は [`docs/merged-branch-guard.md`](docs/merged-branch-guard.md)。）
  - この確認は**無条件**。「直前に自分が push したばかり」「指摘を受けて直しただけ」は
    スキップ理由にならない（この油断での再発が実際に起きている）。
  - **ユーザーの「動作確認してみたら◯◯だった」という報告は、その確認のために PR をマージした
    可能性が高い**。修正に入る前に必ずマージ状態を確認する。
- **PR は完了を待たずに開く**（MUST）。最初の意味のあるコミットができた時点で push して PR を作り、
  以後は**判断根拠・残作業・実行済みの不可逆な外向き操作**を PR 本文に書き足していく。枠切れ・中断は
  予告なく来るので、push されていない作業はコンテナごと消えて**誰も引き継げない**（「なぜ」を完了時の
  タスク履歴だけに頼らないこと——途中で止まったセッションでは実行されない）。→
  [`docs/agent-handoff.md`](docs/agent-handoff.md)
- **別スレッドが途中まで進めた作業を引き継ぐとき**（枠切れ等での続行を頼まれたとき）は、元ブランチに
  乗らず**分岐し、元 PR は引き継ぎ先を書いてから close する**。元スレッドは枠復活や PR 購読の webhook で
  後から動きうるが、close しておけば上の無条件確認が closed を拾って止める。手順:
  [`docs/agent-handoff.md`](docs/agent-handoff.md)。
- **1依頼=1PR**: ユーザーの依頼ごとに最新 `main` から新ブランチを切り、独立した1つの PR にする。
  別の依頼で作ったブランチ（特にマージ済み）に積み足さない。例外はユーザーが「同じPRに含めて」と
  明示した場合のみ。積み足しに気づいたら該当コミットを新ブランチへ分離して別 PR にする。
- 新ブランチを切る前に必ず `git fetch origin main`（コンフリクト予防）。長いセッションでは**基点のほうが
  古くなる**ので、push 前にも基点が `origin/main` の先端か・**触ったファイルが基点以降に変更されて
  いないか**を見る（他セッションが並行で動く。気づかず出すとその PR が相手の変更を巻き戻す）。
- URL に影響する変更・外向き操作は必ずレビュー/確認を求める。
- コミットメッセージは日本語・英語どちらでも可。
- **ファイルのリネーム・転用・統合では、元ファイルの機能一覧を先に列挙**してから作業し、
  「不要化の合意」がない限り既存機能を削除・簡略化しない（意図せぬ機能欠落が起きやすい）。
  削る場合はユーザー合意と代替手段を同一 PR に明記する。

## 作業完了時の必須手順（ユーザーへの完了報告より前に・促される前に自分から）

1. `scripts/new-task-history.mjs <タスクスラッグ> "<短いタイトル>"` で、時刻＋ランダムIDを持つ
   `docs/history-inbox/` の新規ファイルに「**なぜ**」を記録する（規約: [`docs/task-history.md`](docs/task-history.md)。
   本体への統合・アーカイブは ops-sync の自動バッチが行うので手を出さない）。
2. 作業中に実証で誤り・不足と分かったドキュメントをその場で正す（配布 doc なら上りで正本を直す。
   黙って回避策で済ませない）。
3. **推測を断定として報告しない**（MUST NOT）。原因の特定・修正の成功・設定が効いていることは、
   **修正を当てた版を実際に走らせた結果**（run・ログ・実測値）で確かめてから書く。切り分けで分かるのは
   分かったところまでで、その先を埋めない。確かめられないものは「未確認」と区別して書く
   （実例: 1回目の仮説で「直った」と報告したが同じエラーが続いていた）。

## レビュー指摘は resolve までが1セット

**このアカウントのリポジトリで PR をレビューするのは Codex だけ**（人間のレビュアーはいない）。
落とした指摘を後から拾う人がいないので、**未 resolve のスレッド1本＝誰も見ていない欠陥1件**として扱う。
「二次意見だから後回しでよい」「重要なら誰かが再度指摘する」は成り立たない。

**レビュースレッドに触れたら、必ず決着させてから終わる**（MUST）。積み残し消化の依頼に限らず、
**自分の PR に付いた指摘に対応するときも同じ**（発火条件は「指摘に対応した」であって依頼の種類ではない）。

- **直した** → 修正が `main` に入った時点で resolve 済みにする（MUST）。何を直したかがスレッドから自明でなければ返信も添える。
- **対応しない判断をした** → **理由を返信した上で** resolve 済みにする（MUST）。無言の放置は「未対応」として残り続ける。
- **修正が未マージの PR にあるうちは resolve しない**（MUST NOT）。resolve は「直った」の唯一の信号なので、
  先に消すと未修正の欠陥が在庫から消える。マージ後に resolve するところまでを自分の担当に含める。

**上の要求は経路によらない**。満たし方だけがエージェントの権限で変わる:

- **返信・resolve の権限があるなら**自分で行う。
- **権限が無いなら（Codex 等）ユーザーに代行を依頼する**（MUST）。権限が無いことは、スレッドを開いたまま
  黙って終える理由にならない（MUST NOT）。完了報告に**スレッドの URL と、投稿してほしい本文
  （対応しない判断ならその理由）を、そのまま貼れる形で**並べて依頼する（MUST）。
  「権限が無いので対応できませんでした」だけで終えない（MUST NOT）——依頼されなければユーザーは
  代行できず、結局その指摘は誰も見ていない欠陥のまま残る。

**黙って直して resolve しないのが最悪**（MUST NOT）。スレッドは無視されたものと見分けがつかず、
修正済みの指摘が在庫に積み上がり、次のセッションが同じ指摘を一から再調査する
（実例: 93件の未対応在庫を棚卸ししたところ、大半が「既に直っているが未 resolve」だった）。

消化依頼（「Codex の指摘に対応して」等）を受けたら、**一覧の全件を上の3状態のいずれかで終える**（MUST）。
**指摘文だけを見て直し始めない**（MUST NOT）——まず現在の正本を読み、既に直っていないかを確かめる
（上の実例のとおり、古い指摘ほど修正済みの割合が高い。確かめずに直すと二重に触る）。
時間・文脈が尽きて全件を終えられないときは、**残件を明示して報告する**（MUST）。件数だけ稼いで
完了扱いにしない（MUST NOT）——報告の無い未着手は、次のセッションから「誰も見ていない指摘」と区別がつかない。

積み残しの一覧は `.ops-sync/codex-review-inbox.md`（ops-sync が生成する**生成物**・手編集しない）。public repo には
このスライスを置かないため、ファイルが無ければ依頼に添付された全体一覧の当該リポジトリ欄を読む。
**載っている＝未対応**で、直して resolve すれば次回実行で消える。読み方は一覧冒頭。

## CI ログ運用

新規に `.github/workflows/` を追加するときは必ず CI ログ出力（`publish-ci-logs`）を組み込む。
具体的な手順は [`docs/ci-logs.md`](docs/ci-logs.md)。

## 外部ネットワーク取得が要るとき（egress 制限で URL に届かない）

- **発火点は「公開 URL へのアクセスが 403/407 等で弾かれた瞬間」**（WebFetch・curl・ブラウザ等、手段を問わない）。
  その取得が作業に必要なら、GitHub Actions を中継する net-fetch リレーを使う。手順は
  [`docs/net-fetch.md`](docs/net-fetch.md)（使う直前に読む）。
- 対象は**認証不要で公開取得してよいリソースだけ**（クリーンルーム実行で secret を持てない）。
- **集約モード（public な計算基盤 ops-runner 上で実行）の結果は世界公開に落ちる**。機微を取得しうるものは
  public 経路に流さない（→ 手順の「モード」節。private リポジトリの分散モードを使う）。
- **取得先が allowlist に無ければ、停止してユーザーに手動追加を依頼する**（MUST）。勝手に分散モードへ
  切り替えて回避したり、自分で allowlist に足して続行したりしない（MUST NOT）。モードは可視性・機微性で選ぶ。

## private リポジトリの workflow を自分の判断で dispatch するとき

GitHub Actions の月枠は**アカウント単位**で全 private リポジトリが共有し、枠切れの実行は run の失敗に
留まらず spending limit の設定次第で**実費課金**につながる。エージェントが自発的に枠を消費しにいくとき
（net-fetch の分散モードを含む）は、実行前に [`docs/actions-quota.md`](docs/actions-quota.md) の手順で
逼迫状態を確認し、**`ok` 以外なら dispatch しない**（MUST NOT）。信号が無い・古いときは逼迫扱いにする。
ユーザーが明示的に依頼した通常作業（deploy 等）は対象外。
**private リポジトリの定期実行（cron）の頻度・時刻を決める／変えるとき**も同じ doc を読む
（頻度は月間合計ではなく**逼迫時の限界1分**で評価する・時刻は枠リセット直後に寄せる）。

## UI から GitHub Actions を起動するボタンを作るとき

管理画面などの UI に **workflow を起動するコントロール**（ボタン・リンク・フォーム送信）を追加・変更するときは、
**押す前に「Actions の workflow を起動する」と分かる印を必ず付ける**（MUST）。印は「一目で他のボタンと
区別できる見た目」＋「**ホバー不要で読める**テキスト」の2つセットで（`title` だけで済ませない。MUST NOT）、
実装はそのリポジトリの UI 共通部品に1つ置いて参照する（個別ページに直書きしない）。枠消費（アカウント共有の
月枠）に言及してよいのは、その run が**実際に枠を消費するときだけ**（典型は private ✕ GitHub-hosted runner。
可視性だけでは決まらないので `runs-on` を見る）。
**押した時点では run を作らないが、押した後の実行のされ方を変える操作**も対象で、**起動用とも互いにも別の印**を
付ける（MUST。同じ印にすると「印＝いま workflow が走る」が読めなくなる）——(a) **それ以降の実行を課金対象に
変える**（デプロイ経路を Actions 側へ戻すスイッチ等。run を作るのは人の push）、(b) **起動を無人にする**
（cron・キュー処理の「開始」ボタンで、以後その処理**自身が** dispatch し、止めるまで run が作られ続ける）。
(b) は枠を消費するかと**独立**で、消費しないループにも印は要る（その場合の文言は「止めるまで自動で起動する」
だけにし、枠消費を書かない）。**workflow を起動せず (a)(b) のどちらも変えないボタンには付けない**（MUST NOT）。
「cron が挟まるので間接的だ」を対象外の理由にしない（MUST NOT）。
対象判定・印の要件・実描画確認は [`docs/gha-trigger-ui.md`](docs/gha-trigger-ui.md)。

## D1 データベースを扱うとき

新しい D1 データベースを作る、既存 D1 に定常的な書き込み（ログ等）を実装するとき、
**一度きりの移行・是正が本番で完了したか等を確認したいとき**、または**役目を終えた列・テーブルを
本番から消すとき**は、必ず [`docs/d1-operations.md`](docs/d1-operations.md) を読むこと。
D1 の 5GB 容量上限は**アカウント内「全 D1 の合計」**で複数リポジトリ横断の制約になるため、容量の測り方と
retention 実装のハマりどころ（バイトでなく行数で刈る・`OFFSET` 全スキャン禁止 等）を先に把握する。
「本番の実データを確認する手段がない」と結論する前にも同じ doc を読む。
**スキーマの撤去は順序が肝**（コードの撤去を本番へデプロイし、稼働中のコードに参照が無いことを確かめてから
消す。冪等な ensure が生きていると次の実行が作り直す）で、これも同 doc に手順がある。
<!-- OPS-SYNC:COMMON END -->
