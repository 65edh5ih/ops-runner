---
name: gha-trigger-ui
description: 管理画面などの UI に GitHub Actions の workflow を起動するコントロール（ボタン・リンク・フォーム送信）を追加・変更するとき、既存コントロールの起動有無が変わったとき、および workflow を起動しないが押すとそれ以降の実行が Actions の実行枠を消費するようになる操作（デプロイ経路の切替スイッチなど、workflow のゲートに使う設定値を書き換えるもの）を追加・変更するときの表示規約。押す前に「Actions の workflow を起動する」「これ以降の実行が枠を消費するようになる」と分かる印を付けるための手順。
---

`docs/gha-trigger-ui.md` を読み、その手順に従う。
